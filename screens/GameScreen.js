import React from 'react';
import {View, Text, StyleSheet, ToucheAble} from 'react-native';

const GameScreen = props => {
    return(
        <View></View>
    );
};

const styles = StyleSheet.create({
    game: {}
});

export default GameScreen;